version: 2
jobs:
  build:
    docker:
      - image: circleci/python:3.5
    steps:
      - checkout
      - run: |
          python3 -m venv venv
          . ./venv/bin/activate
          pip install sphinx
          make html
      - store_artifacts:
          path: _build/html
